/**
 * UI Components (Atoms) Barrel Export
 *
 * Components in this directory are auto-imported by Nuxt.
 * This file serves as documentation for available atoms.
 *
 * Available:
 * - UButton.vue            — Button with variants, sizes, loading state
 * - UInput.vue             — Input with label, error, password toggle
 * - UAlert.vue             — Alert/feedback banner with variants
 * - UAvatar.vue            — Avatar with image or initials fallback
 * - ULoading.vue           — Loading spinner (inline / block / overlay)
 * - USkeleton.vue          — Base skeleton placeholder (text, circular, rectangular, rounded)
 * - SkeletonCardGrid.vue   — Skeleton grid for list pages (media, profile, article, simple)
 * - SkeletonDetailContent.vue — Skeleton for detail pages (article, profile, default)
 *
 * Planned:
 * - UCard.vue
 * - UModal.vue
 * - UBadge.vue
 * - USelect.vue
 * - UTextarea.vue
 */
export { };

